const { ingredientsRoute } = require("./ingredients");
const { recipesRoute } = require("./recipe");

const { swaggerRoute } = require("./swagger");

module.exports = { swaggerRoute, ingredientsRoute, recipesRoute };
